# 智能体执行规则

本仓库描述的是目标状态，不是允许无条件执行的脚本。智能体必须遵守以下规则。

## 基本流程

1. 执行前读取当前系统状态，不假设软件、文件或配置不存在。
2. 向用户说明本阶段将修改的文件、安装的软件和可能的副作用。
3. 修改现有文件前创建带时间戳的备份。
4. 一次只完成一个章节；运行章节中的验证，成功后再继续。
5. 命令失败时停止当前章节，保留输出并诊断，不重复盲目执行。
6. 不执行与用户目标无关的清理、升级或重构。

## 必须询问用户

遇到以下情况必须暂停并取得明确同意：

- 删除应用、配置或用户数据；
- 修改 `/etc`、`/boot`、EFI、Secure Boot 密钥或启动器；
- 覆盖 `/usr/bin` 下的程序；
- 安装 AUR 软件包或来源未经核对的第三方代码；
- 修改代理、DNS、网络路由或系统级 Chrome 策略；
- 需要订阅链接、密码、令牌、密钥或其他秘密；
- 文档与当前 Omarchy 版本、命令帮助或现有配置不一致。

## Omarchy 安全边界

- 永远不要修改 `/usr/share/omarchy/`；它只可用于读取默认值和命令实现。
- 用户配置应写入 `~/.config/`、`~/.local/` 或 Omarchy 提供的用户级位置。
- 优先使用 `omarchy <group> <action>`，执行前用 `--help` 或 `omarchy commands` 核对当前命令。
- 交互式终端中的提权优先使用 `sudo`；只有无法在终端输入密码的图形执行环境才使用 `pkexec`。
- Hyprland 修改后运行 `hyprctl reload` 和 `hyprctl configerrors`。
- 不得在没有用户确认的情况下运行 `omarchy refresh`、`omarchy reinstall` 或系统更新。

## 秘密与公开仓库

- 不把 Mihomo 订阅地址、控制器 secret、密码、令牌或私钥写入文件或 Git 历史。
- 需要秘密时只在执行阶段询问用户，并避免把完整值显示在日志中。
- 提交前检查暂存内容中是否出现真实秘密或机器专属标识。

## 完成标准

每个章节只有在以下条件全部满足后才算完成：

- 目标状态已经达到；
- 章节验证命令通过；
- 没有未解释的错误或残留临时文件；
- 用户知道发生了哪些修改以及如何回滚。
